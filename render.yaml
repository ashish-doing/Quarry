services:
  - type: web
    name: quarry-hub
    runtime: python
    plan: free
    branch: hub-hosting
    buildCommand: pip install -r requirements-hub.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: QUARRY_JOIN_CODE
        sync: false